<!--pages/place/place.wxml-->
<view class="trip">
  <view class="trip-top" wx:if="{{ trip != null }}">
    <view class="trip-info">
      <view class="h1">{{ trip.aname }}</view>
      <text>{{trip.description}}</text>
    </view>
  </view>
</view>

